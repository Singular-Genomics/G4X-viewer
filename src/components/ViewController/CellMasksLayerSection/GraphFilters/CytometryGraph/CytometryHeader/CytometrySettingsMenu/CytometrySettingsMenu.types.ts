import { ColorScale } from 'plotly.js';

export type ColorScaleOption = {
  label: string;
  value: ColorScale;
  reversed: boolean;
};

export type SelectOption = {
  label: string;
  value: string;
};
